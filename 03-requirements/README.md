# 03 — Requirements

Register authoritative sources, preserve source-defined identifiers, analyze ambiguity and conflicts, and create traceability metadata. Do not silently rewrite source requirements.

If a source has no stable IDs, propose `LOCAL-REQ-NNN` aliases and retain exact source locations. A requirements baseline is approved independently from Discovery.

