# Codex Adapter

Start in planning mode for design work:

```text
Read solution-design-playbook/README.md and run prompt 000.
Inspect current project files read-only where relevant.
Maintain docs/project-design YAML and Markdown consistently.
Do not implement until an explicit approved phase authorizes it.
```

Use available file, browser, document, or connector tools only within the current stage and user authorization. Completion never approves the next phase.

