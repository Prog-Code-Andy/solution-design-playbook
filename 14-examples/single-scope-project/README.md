# Single-Scope Example

An internal documentation portal has one cohesive owner, deployment path, and design boundary. MAIN is sufficient; no component scopes are created.

Expected output root: `docs/project-design/`.

