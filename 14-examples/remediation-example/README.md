# Remediation Example

A previously completed integration fails to recover from a confirmed timeout and lacks diagnostic evidence. The durable diagnostic policy remains valid, so no new EDR is required; a bounded REM repairs the gap. If investigation instead shows that accepted policy must change, a new EDR precedes the REM.

